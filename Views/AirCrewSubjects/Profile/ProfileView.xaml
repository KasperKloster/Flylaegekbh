<UserControl x:Class="FlyveLægeKBH.Views.AirCrewSubjects.Profile.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlyveLægeKBH.Views.AirCrewSubjects.Profile"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
       
        
        <StackPanel Margin="20,20,500,20">
            <TextBox Style="{StaticResource searchBar}" x:Name="Get_all_info_test_tbx" Text="{Binding SocialSecurityNUmber}" Width="261" Padding="10"/>
            <Button Style="{StaticResource ctaButton}" x:Name="Get_all_info_test_btn" Content="Get all info"   Command="{Binding Path=GetAllInfoCommand}" CommandParameter="{Binding}"/>
        </StackPanel>
        
        <!-- this could also represent a childview that changes depending on what we click on the right side of the view-->
        <StackPanel Margin="430,20,20,30">
            <TextBlock Style="{StaticResource headline2}" x:Name="Get_all_info_test_card" Text="{Binding UserInfo}"/>
        </StackPanel>
    </Grid>
</UserControl>
